services:
  mlops-api:
    image: python:3.10-slim
    container_name: lab6_mlops_api
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             uvicorn service.main:app --host 0.0.0.0 --port 8000"
    ports:
      - "8000:8000"
